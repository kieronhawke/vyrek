import type { SchemaTypeDefinition } from "sanity";

// Schemas are added in Phase G (Sanity + Blog + SEO).
// Until then this is intentionally empty so the Studio loads with no document types.
export const schemaTypes: SchemaTypeDefinition[] = [];
